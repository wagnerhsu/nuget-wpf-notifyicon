<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:TaskbarIcon05"
    xmlns:local1="clr-namespace:TaskbarIcon05.ViewModels"
    xmlns:tb="http://www.hardcodet.net/taskbar">
    <ContextMenu x:Key="SysTrayMenu" x:Shared="false">
        <MenuItem Command="{Binding ShowWindowCommand}" Header="Show Window" />
        <Separator />
        <MenuItem Command="{Binding ExitApplicationCommand}" Header="Exit" />
    </ContextMenu>


    <!--  the application's NotifyIcon - started from App.xaml.cs. Declares its own view model.  -->
    <tb:TaskbarIcon
        x:Key="NotifyIcon"
        ContextMenu="{StaticResource SysTrayMenu}"
        DoubleClickCommand="{Binding ShowWindowCommand}"
        IconSource="/Red.ico"
        ToolTipText="Double-click for window, right-click for menu">

        <!--  self-assign a data context (could also be done programmatically)  -->
        <tb:TaskbarIcon.DataContext>
            <local1:NotifyIconViewModel />
        </tb:TaskbarIcon.DataContext>
    </tb:TaskbarIcon>

</ResourceDictionary>